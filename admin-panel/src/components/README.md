# components/

Cross-feature reusable UI primitives (Button, Input, Modal, Table, etc.).
Feature-local components belong inside `src/features/<name>/components/`,
not here. The rule of thumb: if more than one feature uses it, promote
it to this folder.

Empty for now — populate as the design system takes shape.
